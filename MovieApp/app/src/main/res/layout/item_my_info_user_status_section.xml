<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools">
    <data>
        <import type="android.view.View" />
        <variable
            name="icon_src"
            type="android.graphics.drawable.Drawable" />
        <variable
            name="section_title"
            type="String" />
        <variable
            name="sub_title"
            type="String" />
        <variable
            name="badge_text"
            type="String" />
    </data>
    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="4dp"
        android:paddingVertical="12dp"
        android:layout_marginHorizontal="4dp"
        android:layout_marginVertical="4dp"
        android:orientation="horizontal"
        android:gravity="center"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?attr/selectableItemBackground"
        android:stateListAnimator="@animator/item_press_scale"
        tools:showIn="@layout/fragment_my_info">

        <FrameLayout android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="@drawable/rounded_icon_background">

            <ImageView
                    android:id="@+id/my_info_icon"
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:layout_gravity="center"
                    app:tint="@color/text_primary"
                    tools:src="@tools:sample/avatars"/>
        </FrameLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginHorizontal="16dp"
            android:orientation="vertical">

            <TextView xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{section_title}"
                android:fontFamily="@font/dm_sans_semibold"
                android:textSize="@dimen/text_sm"
                android:textColor="@color/text_primary"
                tools:text="My Bookings"/>

            <TextView xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{sub_title}"
                android:visibility="@{sub_title != null &amp;&amp; !sub_title.empty ? View.VISIBLE : View.GONE}"
                android:fontFamily="@font/dm_sans_regular"
                android:textSize="@dimen/text_xs"
                android:layout_marginTop="2dp"
                android:textColor="@color/seat_selected"
                tools:text="View your booking history"/>
        </LinearLayout>

        <include
            layout="@layout/badge_text_circle"
            android:visibility="@{badge_text != null &amp;&amp; !badge_text.empty ? View.VISIBLE : View.GONE}"
            app:text="@{badge_text}" />

        <ImageButton
            android:background="@color/transparent"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:layout_marginStart="8dp"
            android:src="@drawable/chevron_forward_24px" />
    </LinearLayout>
</layout>
